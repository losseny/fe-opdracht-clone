import {css} from "lit";

export const HomePageStyles = css `

    .content-wrap {
        display: flex;
        gap: 2rem;
        flex-direction: column;
    }
    
    .content-container {
        padding: 1rem;
    }
`