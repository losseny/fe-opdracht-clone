import {css} from "lit";

export const HomePageStyles = css `

    .content-wrap {
        display: flex;
        gap: 2rem;
        flex-direction: column;
    }
    
    .content-container {
        padding: 1rem;
    }
    .statistics {
        display: flex;
        flex-direction: row;
        justify-content: center;
        align-items: center;
        gap: 3rem;
        margin-top: 3.4rem;
        margin-bottom: 3.4rem;
    }
`